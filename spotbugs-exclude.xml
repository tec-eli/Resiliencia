<?xml version="1.0" encoding="UTF-8"?>
<FindBugsFilter xmlns="https://github.com/spotbugs/filter/3.0.0"
                 xsi:schemaLocation="https://github.com/spotbugs/filter/3.0.0 https://raw.githubusercontent.com/spotbugs/spotbugs/master/spotbugs/etc/findbugsfilter.xsd"
                 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

    <!--
        Records that wrap a Throwable (e.g. Outcome.Failure) are flagged as exposing/storing an internal
        representation, but Throwable identity is exactly what callers need — copying it would break
        stack traces and exception chaining. Excluded library-wide rather than per-occurrence.
    -->
    <Match>
        <Bug pattern="EI_EXPOSE_REP" />
        <Class name="~.*" />
        <Field type="java.lang.Throwable" />
    </Match>
    <Match>
        <Bug pattern="EI_EXPOSE_REP2" />
        <Class name="~.*" />
        <Field type="java.lang.Throwable" />
    </Match>

    <!--
        List-typed record components (e.g. the `listeners` field on every pattern) are defensively
        copied with List.copyOf in the compact constructor, so the returned reference is already
        unmodifiable — SpotBugs can't see through that factory call to know it's safe.
    -->
    <Match>
        <Bug pattern="EI_EXPOSE_REP" />
        <Class name="~.*" />
        <Field type="java.util.List" />
    </Match>

</FindBugsFilter>
