<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
        child.project.url.inherit.append.path="false">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.tec-eli</groupId>
    <artifactId>resiliencia</artifactId>
    <version>1.0.0-beta.1</version>
    <packaging>pom</packaging>

    <name>resiliencia</name>
    <description>A modular resilience patterns library for Java 21+, built on virtual threads and the Java Module
        System: Retry, Timeout, CircuitBreaker, Bulkhead, RateLimiter, and fluent policy composition.
    </description>
    <url>https://github.com/tec-eli/resiliencia</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>tec-eli</id>
            <name>Eliana Moran</name>
            <email>tec-eli@users.noreply.github.com</email>
            <url>https://github.com/tec-eli</url>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/tec-eli/resiliencia.git</connection>
        <developerConnection>scm:git:https://github.com/tec-eli/resiliencia.git</developerConnection>
        <url>https://github.com/tec-eli/resiliencia</url>
        <tag>HEAD</tag>
    </scm>

    <modules>
        <module>resiliencia-core</module>
        <module>resiliencia-patterns</module>
        <module>resiliencia-compose</module>
        <module>resiliencia-metrics</module>
        <module>resiliencia-micrometer</module>
        <module>resiliencia-opentelemetry</module>
        <module>resiliencia-spring</module>
        <module>resiliencia-quarkus</module>
        <module>resiliencia-micronaut</module>
        <module>resiliencia-test</module>
        <module>resiliencia-stress</module>
        <module>resiliencia-examples</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Test -->
        <junit.version>6.1.1</junit.version>

        <!-- Logging -->
        <slf4j.version>2.0.18</slf4j.version>

        <!-- Plugins -->
        <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.5.6</maven-surefire-plugin.version>
        <maven-source-plugin.version>3.3.1</maven-source-plugin.version>
        <maven-javadoc-plugin.version>3.12.0</maven-javadoc-plugin.version>
        <spotbugs-maven-plugin.version>4.8.6.6</spotbugs-maven-plugin.version>
        <pmd-maven-plugin.version>3.25.0</pmd-maven-plugin.version>
        <jacoco-maven-plugin.version>0.8.12</jacoco-maven-plugin.version>
        <central-publishing-maven-plugin.version>0.10.0</central-publishing-maven-plugin.version>
        <maven-gpg-plugin.version>3.2.8</maven-gpg-plugin.version>

        <!--
            resiliencia-examples and resiliencia-stress carry no unit tests by design (see their
            jacoco.skip properties): examples is illustrative usage code, stress is a jcstress
            harness. Without this, SonarCloud counts their source lines as uncovered, dragging
            down the coverage metric for modules that were never meant to have coverage.
        -->
        <sonar.coverage.exclusions>resiliencia-examples/src/main/**,resiliencia-stress/src/main/**</sonar.coverage.exclusions>

        <!--
            site/static/apidocs is committed, generated Oracle JDK javadoc-tool output (regenerated
            and overwritten on every page deploy via javadoc:aggregate, see .github/workflows/pages.yml).
            It is not project source, so Sonar's HTML/JS rules on it (e.g. the tool's own `<html lang>`
            with no value, its `var`-based scripts) are not actionable findings.
        -->
        <sonar.exclusions>site/static/apidocs/**</sonar.exclusions>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit.version}</version>
            </dependency>
            <dependency>
                <groupId>org.assertj</groupId>
                <artifactId>assertj-core</artifactId>
                <version>3.27.7</version>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-simple</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
            </plugin>

            <!--
                Publishes to Sonatype's Central Publisher Portal (the OSSRH/Nexus staging flow it
                replaces is sunset). extensions=true makes it the deployment mechanism for `mvn deploy`;
                per-module publishing is controlled by the existing maven.deploy.skip flags, which it
                honors the same as the default deploy provider. autoPublish is deliberately false: a
                deployment lands as a staged, reviewable bundle in the Portal and must be published
                manually, since Central artifacts are immutable once released.
            -->
            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
                <version>${central-publishing-maven-plugin.version}</version>
                <extensions>true</extensions>
                <configuration>
                    <publishingServerId>central</publishingServerId>
                    <autoPublish>false</autoPublish>
                </configuration>
            </plugin>
        </plugins>

        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <release>${java.version}</release>
                        <compilerArgs>
                            <arg>-parameters</arg>
                        </compilerArgs>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven-source-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>attach-sources</id>
                            <goals>
                                <goal>jar-no-fork</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${maven-javadoc-plugin.version}</version>
                    <configuration>
                        <release>${java.version}</release>
                        <bottom>Copyright &#169; 2026 Resiliencia. All rights reserved.</bottom>
                    </configuration>
                    <executions>
                        <execution>
                            <id>attach-javadocs</id>
                            <goals>
                                <goal>jar</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- Static analysis; run explicitly in CI (mvn spotbugs:check / pmd:check), not bound to the default build. -->
                <plugin>
                    <groupId>com.github.spotbugs</groupId>
                    <artifactId>spotbugs-maven-plugin</artifactId>
                    <version>${spotbugs-maven-plugin.version}</version>
                    <configuration>
                        <excludeFilterFile>${maven.multiModuleProjectDirectory}/spotbugs-exclude.xml</excludeFilterFile>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-pmd-plugin</artifactId>
                    <version>${pmd-maven-plugin.version}</version>
                    <configuration>
                        <targetJdk>${java.version}</targetJdk>
                    </configuration>
                </plugin>

            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <!--
            Activated explicitly (-P release) by the release workflow only. Signing is gated behind
            a profile rather than bound unconditionally so that local `mvn install`/`mvn verify`
            (which run the `verify` phase) don't fail for contributors without a GPG key configured.
        -->
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>${maven-gpg-plugin.version}</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
